<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GRC Agent - Análise de Frameworks</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            background: white;
            border-radius: 12px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            overflow: hidden;
        }

        .header {
            background: linear-gradient(135deg, #2c3e50 0%, #34495e 100%);
            color: white;
            padding: 30px;
            text-align: center;
        }

        .header h1 {
            font-size: 2.5rem;
            margin-bottom: 10px;
        }

        .header p {
            font-size: 1.1rem;
            opacity: 0.9;
        }

        .main-content {
            padding: 40px;
        }

        .config-warning {
            background: #f39c12;
            color: white;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 30px;
            text-align: center;
        }

        .form-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin-bottom: 30px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
        }

        .form-group.full-width {
            grid-column: 1 / -1;
        }

        label {
            font-weight: 600;
            margin-bottom: 8px;
            color: #2c3e50;
            font-size: 0.95rem;
        }

        select, input, textarea {
            padding: 12px;
            border: 2px solid #e1e8ed;
            border-radius: 8px;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        select:focus, input:focus, textarea:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        textarea {
            min-height: 120px;
            resize: vertical;
        }

        .functionality-cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-bottom: 30px;
        }

        .functionality-card {
            border: 2px solid #e1e8ed;
            border-radius: 8px;
            padding: 20px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-align: center;
            background: #f8f9fa;
        }

        .functionality-card:hover {
            border-color: #667eea;
            background: #f0f2ff;
        }

        .functionality-card.selected {
            border-color: #667eea;
            background: #667eea;
            color: white;
        }

        .functionality-card h3 {
            font-size: 1.1rem;
            margin-bottom: 8px;
        }

        .functionality-card p {
            font-size: 0.9rem;
            opacity: 0.8;
        }

        .submit-btn {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            padding: 15px 40px;
            font-size: 1.1rem;
            font-weight: 600;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
        }

        .submit-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
        }

        .submit-btn:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none;
        }

        .loading {
            display: none;
            text-align: center;
            padding: 20px;
        }

        .spinner {
            width: 40px;
            height: 40px;
            border: 4px solid #e1e8ed;
            border-left: 4px solid #667eea;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin: 0 auto 20px;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        .results {
            display: none;
            margin-top: 30px;
            padding: 30px;
            border: 2px solid #e1e8ed;
            border-radius: 8px;
            background: #f8f9fa;
        }

        .results h3 {
            color: #2c3e50;
            margin-bottom: 20px;
            font-size: 1.3rem;
        }

        .result-json {
            background: #2c3e50;
            color: #00ff88;
            padding: 20px;
            border-radius: 8px;
            font-family: 'Courier New', monospace;
            font-size: 0.9rem;
            white-space: pre-wrap;
            overflow-x: auto;
            max-height: 500px;
            overflow-y: auto;
        }

        .download-btn {
            background: #27ae60;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 15px;
            font-weight: 600;
        }

        .download-btn:hover {
            background: #229954;
        }

        .error {
            background: #e74c3c;
            color: white;
            padding: 15px;
            border-radius: 8px;
            margin-top: 20px;
            display: none;
        }

        @media (max-width: 768px) {
            .form-grid {
                grid-template-columns: 1fr;
            }
            
            .functionality-cards {
                grid-template-columns: 1fr;
            }
            
            .header h1 {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🛡️ GRC Agent</h1>
            <p>Análise Inteligente de Frameworks de Segurança</p>
        </div>

        <div class="main-content">
            <!-- Configuração ativa -->
            <div style="background: #27ae60; color: white; padding: 15px; border-radius: 8px; margin-bottom: 30px; text-align: center;">
                ✅ <strong>SISTEMA CONFIGURADO E PRONTO PARA USO</strong>
            </div>

            <form id="grcForm">
                <div class="form-group full-width">
                    <label>Selecione a Funcionalidade:</label>
                    <div class="functionality-cards">
                        <div class="functionality-card" data-func="A">
                            <h3>📋 Questões de Mapeamento</h3>
                            <p>Gera questões para avaliar controles</p>
                        </div>
                        <div class="functionality-card" data-func="B">
                            <h3>⚠️ Análise de Riscos</h3>
                            <p>Identifica riscos no pior cenário</p>
                        </div>
                        <div class="functionality-card" data-func="C">
                            <h3>🔍 Vulnerabilidades</h3>
                            <p>Mapeia vulnerabilidades técnicas</p>
                        </div>
                        <div class="functionality-card" data-func="D">
                            <h3>📖 Revisão de Política</h3>
                            <p>Revisa políticas existentes</p>
                        </div>
                        <div class="functionality-card" data-func="E">
                            <h3>📄 Nova Política</h3>
                            <p>Cria novas políticas</p>
                        </div>
                    </div>
                </div>

                <div class="form-grid">
                    <div class="form-group">
                        <label for="framework">Framework:</label>
                        <select id="framework" required>
                            <option value="">Selecione um framework</option>
                            <option value="27001">ISO 27001</option>
                            <option value="27002">ISO 27002</option>
                            <option value="27005">ISO 27005</option>
                            <option value="42001">ISO 42001</option>
                            <option value="NIST">NIST Cybersecurity Framework</option>
                            <option value="COSO">COSO ERM</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="controle">Controle:</label>
                        <input type="text" id="controle" placeholder="Ex: A.5.1, ID.AM-1, TODOS" required>
                        <small style="color: #666; margin-top: 4px;">Digite "TODOS" para todos os controles do framework</small>
                    </div>
                </div>

                <div class="form-group full-width">
                    <label for="contexto">Contexto da Empresa:</label>
                    <textarea id="contexto" placeholder="Descreva o contexto: setor, tamanho, características especiais..." required></textarea>
                </div>

                <div class="form-group full-width" id="politica-group" style="display: none;">
                    <label for="conteudo_politica">Conteúdo da Política (para revisão):</label>
                    <textarea id="conteudo_politica" placeholder="Cole aqui o conteúdo da política a ser revisada..."></textarea>
                </div>

                <button type="submit" class="submit-btn">
                    Analisar Framework
                </button>
            </form>

            <div class="loading" id="loading">
                <div class="spinner"></div>
                <p>Processando análise com IA... Aguarde alguns instantes.</p>
            </div>

            <div class="error" id="error"></div>

            <div class="results" id="results">
                <h3>Resultado da Análise</h3>
                <div class="result-json" id="resultJson"></div>
                <button class="download-btn" onclick="downloadResult()">
                    💾 Download JSON
                </button>
            </div>
        </div>
    </div>

    <script>
        // URL do Logic App configurada
        const LOGIC_APP_URL = 'https://prod-54.eastus.logic.azure.com:443/workflows/ec5a9a7b95fa4c46962901069af372a8/triggers/When_an_HTTP_request_is_received/paths/invoke?api-version=2016-10-01&sp=%2Ftriggers%2FWhen_an_HTTP_request_is_received%2Frun&sv=1.0&sig=YlRDzSyrRjlf81yqalIUFfiROvRjogvaBWMo_O5m81k';

        let selectedFunctionality = '';
        let lastResult = null;

        // Seleção de funcionalidade
        document.querySelectorAll('.functionality-card').forEach(card => {
            card.addEventListener('click', function() {
                document.querySelectorAll('.functionality-card').forEach(c => c.classList.remove('selected'));
                this.classList.add('selected');
                selectedFunctionality = this.dataset.func;
                
                // Mostrar/ocultar campo de política
                const politicaGroup = document.getElementById('politica-group');
                if (selectedFunctionality === 'D') {
                    politicaGroup.style.display = 'block';
                    document.getElementById('conteudo_politica').required = true;
                } else {
                    politicaGroup.style.display = 'none';
                    document.getElementById('conteudo_politica').required = false;
                }
            });
        });

        // Submit do formulário
        document.getElementById('grcForm').addEventListener('submit', async function(e) {
            e.preventDefault();
            
            // Validar se URL foi configurada
            if (LOGIC_APP_URL === 'COLE_AQUI_A_URL_DO_SEU_LOGIC_APP') {
                alert('⚠️ Erro: Configure a URL do Logic App no código JavaScript antes de usar!');
                return;
            }
            
            if (!selectedFunctionality) {
                alert('Por favor, selecione uma funcionalidade.');
                return;
            }

            const formData = {
                funcionalidade: selectedFunctionality,
                framework: document.getElementById('framework').value,
                controle: document.getElementById('controle').value,
                contexto: document.getElementById('contexto').value
            };

            // Adicionar conteúdo da política se for funcionalidade D
            if (selectedFunctionality === 'D') {
                formData.conteudo_politica = document.getElementById('conteudo_politica').value;
            }

            // Mostrar loading
            document.getElementById('loading').style.display = 'block';
            document.getElementById('results').style.display = 'none';
            document.getElementById('error').style.display = 'none';
            document.querySelector('.submit-btn').disabled = true;

            try {
                console.log('Enviando dados:', formData);

                const response = await fetch(LOGIC_APP_URL, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(formData)
                });

                if (!response.ok) {
                    throw new Error(`Erro HTTP: ${response.status} - ${response.statusText}`);
                }

                const result = await response.json();
                console.log('Resposta recebida:', result);
                lastResult = result;
                
                // Mostrar resultado formatado
                document.getElementById('resultJson').textContent = JSON.stringify(result, null, 2);
                document.getElementById('results').style.display = 'block';
                
            } catch (error) {
                console.error('Erro completo:', error);
                document.getElementById('error').innerHTML = `
                    <strong>Erro na análise:</strong><br>
                    ${error.message}<br>
                    <small>Verifique o console do navegador (F12) para mais detalhes.</small>
                `;
                document.getElementById('error').style.display = 'block';
            } finally {
                // Ocultar loading
                document.getElementById('loading').style.display = 'none';
                document.querySelector('.submit-btn').disabled = false;
            }
        });

        // Download do resultado
        function downloadResult() {
            if (!lastResult) return;
            
            const filename = `grc-analysis-${selectedFunctionality}-${new Date().toISOString().slice(0,19).replace(/[:.]/g, '-')}.json`;
            const blob = new Blob([JSON.stringify(lastResult, null, 2)], { type: 'application/json' });
            const url = URL.createObjectURL(blob);
            const a = document.createElement('a');
            a.href = url;
            a.download = filename;
            document.body.appendChild(a);
            a.click();
            document.body.removeChild(a);
            URL.revokeObjectURL(url);
        }

        // Verificar configuração na inicialização
        window.addEventListener('load', function() {
            if (LOGIC_APP_URL === 'COLE_AQUI_A_URL_DO_SEU_LOGIC_APP') {
                console.warn('⚠️ ATENÇÃO: Configure a URL do Logic App antes de usar!');
            }
        });
    </script>
</body>
</html>
